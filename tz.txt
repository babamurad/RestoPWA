cd /home/u960938720/tmtourism.com/public/restopwa
git pull
chmod -R 755 storage bootstrap/cache
php artisan cache:clear 
php artisan config:clear 
php artisan view:clear 
php artisan route:clear 


P0 (критично, чтобы этап считать завершённым)
Fullscreen карта в живом checkout UI (не только в ТЗ).
В документации требование есть, но в текущем Blade-UI нет явного CTA/потока “Открыть карту на весь экран” с confirm/cancel и атомарным применением точки. 

Пошаговая валидация контактов до перехода между шагами.
Сейчас блок “Контакты” расположен на шаге 3; надо не допускать ситуацию “узнал об ошибке телефона слишком поздно”. Это вы сами справедливо отмечали — в плане это уже отражено, осталось внедрить в коде. 

Сквозные события телеметрии из ТЗ.
В ТЗ зафиксированы geolocate_* и map_fullscreen_*; нужно фактически эмитить их в runtime и собирать в аналитике. 

P1 (сильный апгрейд качества)
Feature flag для geolocate/fullscreen rollout (10→50→100).
В спецификации это обязательный механизм безопасного запуска; в runtime-конфиге/использовании пока не видно. 

Нормализованное сохранение источника и статуса геолокации в order metadata.
В ТЗ заданы поля address_source, geolocate_status, geolocate_accuracy_m; нужно гарантировать сохранение и использование в отчётах/поддержке. 

UX-полировка мобильного fullscreen (safe-area, клавиатура, жесты).
Это отдельно обозначено в ТЗ как NFR и этап реализации. 